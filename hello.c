#include <stdio.h>

/* hello.c -> gcc package(c preprosessor -> c compiler) ->
   binutils(binary utils) package(assembler -> linker) -> a.out(assembler output) */
int main() {
  printf("Hello, world!\n");
  return 123;
}
